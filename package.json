{
  "name": "@thesis-co/tally-extension",
  "version": "0.0.1",
  "description": "Tally, the community owned and operated Web3 wallet.",
  "main": "index.js",
  "repository": "git@github.com:thesis/tally-extension.git",
  "author": "Matt Luongo <matt@thesis.co>",
  "license": "GPL-3.0",
  "keywords": [
    "ethereum",
    "bitcoin",
    "cryptocurrency",
    "wallet",
    "web3",
    "dapp"
  ],
  "scripts": {
    "start": "DEV_MODE=true yarn build:platforms:chrome",
    "build": "echo STILL NEED BUILD",
    "dist": "yarn build prod",
    "prebuild": "bash build-scripts/prebuild.sh",
    "build:development": "node dev/build/index.js",
    "build:prod": "echo STILL NEED BUILD",
    "build:frontend": "echo STILL NEED BUILD",
    "build:background": "node build-scripts/background",
    "build:manifest": "node build-scripts/manifest",
    "build:platforms:all": "yarn build:platforms:chrome && yarn build:platforms:safari && build:platforms:firefox && build:platforms:opera",
    "build:platforms:chrome": "PLATFORM_BUILD=chrome yarn prebuild platform chrome && yarn build:manifest && yarn build:background",
    "build:platforms:firefox": " PLATFORM_BUILD=firefox yarn prebuild platform firefox && PLATFORM_BUILD=firefox yarn build:manifest && yarn build:background",
    "build:platforms:safari": " PLATFORM_BUILD=safari yarn prebuild platform safari && PLATFORM_BUILD=safari yarn build:manifest && yarn build:background",
    "build:platforms:opera": " PLATFORM_BUILD=opera yarn prebuild platform opera && PLATFORM_BUILD=opera yarn build:manifes && yarn build:background"
  },
  "stylelint": {
    "extends": "stylelint-config-xo"
  },
  "devDependencies": {
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.0",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "browserify-livereload": "^1.0.10",
    "envify": "^4.1.0",
    "watchify": "^4.0.0"
  },
  "dependencies": {
    "@babel/plugin-transform-typescript": "^7.13.0"
  }
}
